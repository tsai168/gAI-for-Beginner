# CPO AI frontend — env template (WBS-B10, ADR-0022). Copy to .env.local
# (Vite convention; already covered by the repo's .gitignore via .env.*).
# Only VITE_-prefixed vars are exposed to client code (Vite requirement).

VITE_API_BASE_URL=
