# CPO AI — 環境變數樣板（僅列名，不填值；複製為 .env 後填入）
# .env 已被 .gitignore 排除，不得提交（CLAUDE.md §9）。

# --- 應用 ---
APP_ENV=
LOG_LEVEL=

# --- PostgreSQL (I01) + pgvector (I05)，同一實例 ---
DATABASE_URL=
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_DB=
POSTGRES_USER=
POSTGRES_PASSWORD=

# --- 物件／快照儲存 (I02) ---
OBJECT_STORAGE_ENDPOINT=
OBJECT_STORAGE_BUCKET=
OBJECT_STORAGE_ACCESS_KEY=
OBJECT_STORAGE_SECRET_KEY=
OBJECT_STORAGE_REGION=

# --- 快取 (I03)；產品待 B5 定 ---
CACHE_URL=

# --- Temporal (I04)；自架 vs Cloud 待部署定 ---
TEMPORAL_HOST_URL=
TEMPORAL_NAMESPACE=
TEMPORAL_TASK_QUEUE=
TEMPORAL_TLS_CERT=
TEMPORAL_TLS_KEY=

# --- LLM：供應商 = Claude (Anthropic)；model id／budget 為 Deferred，V1 留空 ---
ANTHROPIC_API_KEY=
LLM_MODEL_ID=
LLM_MAX_OUTPUT_TOKENS=
LLM_DAILY_TOKEN_BUDGET=

# --- API 認證 (OIDC/JWT，ADR-0004) ---
OIDC_ISSUER=
OIDC_AUDIENCE=
OIDC_JWKS_URL=

# --- API CORS（WBS-B10，ADR-0022；逗號分隔 origin，留空=不啟用 CORS） ---
API_CORS_ORIGINS=

# --- 排程／批次（Deferred，Charter §29；V1 一律留空，不得寫死） ---
WAKEUP_WINDOWS=
DAILY_SNAPSHOT_TIME=
BATCH_SIZE=
PRIORITY_WAKEUP_THRESHOLD=

# --- Ingestion 來源 (D01–D06)；認證方式待 B2 Data Availability Audit ---
TWSE_BASE_URL=
TPEX_BASE_URL=
MOPS_BASE_URL=
MARKET_DATA_API_BASE_URL=
MARKET_DATA_API_KEY=
TDCC_BASE_URL=

# --- D08 授權付費來源：V1 停用（Work-1 TQ-05），留空 ---
D08_ENABLED=
