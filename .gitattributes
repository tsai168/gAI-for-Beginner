# Normalize line endings to LF in the repo (Windows checkouts still get CRLF
# locally if core.autocrlf=true, but the stored blobs stay LF).
* text=auto eol=lf
*.png binary
*.jpg binary
*.docx binary
*.pdf binary
